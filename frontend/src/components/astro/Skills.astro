---
// At build time, we use static data. In production, this would fetch from API.
const skillGroups = [
  {
    category: "AI & Machine Learning",
    skills: [
      { name: "LLMs / RAG / GenAI", proficiency: 90 },
      { name: "XGBoost / Scikit-learn", proficiency: 88 },
      { name: "PyTorch", proficiency: 85 },
      { name: "NLP / TF-IDF", proficiency: 85 },
      { name: "HuggingFace", proficiency: 80 },
    ],
  },
  {
    category: "Languages & Frameworks",
    skills: [
      { name: "Python (Django, FastAPI)", proficiency: 95 },
      { name: "SQL (PostgreSQL, SQL Server)", proficiency: 90 },
      { name: "Pandas / Polars / PySpark", proficiency: 92 },
      { name: "JavaScript / TypeScript", proficiency: 75 },
      { name: "Angular / React / Next.js", proficiency: 70 },
    ],
  },
  {
    category: "Data Engineering",
    skills: [
      { name: "Apache Airflow", proficiency: 90 },
      { name: "Celery (Workers)", proficiency: 88 },
      { name: "ETL / ELT Pipelines", proficiency: 92 },
      { name: "Docker", proficiency: 85 },
      { name: "Data Quality / Validation", proficiency: 88 },
    ],
  },
  {
    category: "Cloud & Infrastructure",
    skills: [
      { name: "Azure (Container Apps, ACR)", proficiency: 88 },
      { name: "Azure OpenAI Service", proficiency: 85 },
      { name: "Azure Blob / PostgreSQL", proficiency: 85 },
      { name: "CI/CD (Azure DevOps, Git)", proficiency: 82 },
      { name: "Power BI / Grafana", proficiency: 75 },
    ],
  },
];
---

<section class="skills section" id="skills">
  <div class="container">
    <h2 class="section-title" data-animate="fade-up">Skills & Technologies</h2>
    <p class="section-subtitle" data-animate="fade-up">
      Tools and technologies I work with daily
    </p>

    <div class="skills__grid" data-animate-stagger>
      {skillGroups.map((group) => (
        <div class="skills__group">
          <h3 class="skills__group-title">{group.category}</h3>
          <div class="skills__list">
            {group.skills.map((skill) => (
              <div class="skills__item">
                <div class="skills__item-header">
                  <span class="skills__item-name">{skill.name}</span>
                  <span class="skills__item-percent">{skill.proficiency}%</span>
                </div>
                <div class="skills__bar">
                  <div
                    class="skills__bar-fill"
                    style={`--target-width: ${skill.proficiency}%`}
                  />
                </div>
              </div>
            ))}
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .skills__grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-2xl);
  }

  .skills__group {
    padding: var(--space-xl);
    border-radius: var(--radius-lg);
    background: var(--color-bg-card);
    border: 1px solid var(--color-border);
    transition: border-color var(--transition-normal);
  }

  .skills__group:hover {
    border-color: var(--color-accent);
  }

  .skills__group-title {
    font-size: var(--font-size-lg);
    font-weight: 700;
    color: var(--color-accent);
    margin-bottom: var(--space-lg);
    font-family: var(--font-mono);
  }

  .skills__list {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .skills__item-header {
    display: flex;
    justify-content: space-between;
    margin-bottom: var(--space-xs);
  }

  .skills__item-name {
    font-size: var(--font-size-sm);
    font-weight: 500;
    color: var(--color-text-primary);
  }

  .skills__item-percent {
    font-size: var(--font-size-xs);
    font-family: var(--font-mono);
    color: var(--color-text-muted);
  }

  .skills__bar {
    width: 100%;
    height: 6px;
    background: var(--color-bg-secondary);
    border-radius: var(--radius-full);
    overflow: hidden;
  }

  .skills__bar-fill {
    height: 100%;
    width: 0;
    background: linear-gradient(90deg, var(--color-accent), var(--color-accent-secondary));
    border-radius: var(--radius-full);
    transition: width 1s var(--ease-out-expo);
  }

  .is-visible .skills__bar-fill {
    width: var(--target-width);
  }

  @media (max-width: 768px) {
    .skills__grid {
      grid-template-columns: 1fr;
    }
  }
</style>
